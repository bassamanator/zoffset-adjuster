name: Build and Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'

jobs:
  build:
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: zoffset-adjuster
            artifact: zoffset-adjuster-x86_64-unknown-linux-gnu
            use_cross: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: zoffset-adjuster.exe
            artifact: zoffset-adjuster-x86_64-pc-windows-msvc.exe
            use_cross: false
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: zoffset-adjuster
            artifact: zoffset-adjuster-x86_64-apple-darwin
            use_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: zoffset-adjuster
            artifact: zoffset-adjuster-aarch64-apple-darwin
            use_cross: false
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary: zoffset-adjuster
            artifact: zoffset-adjuster-aarch64-unknown-linux-gnu
            use_cross: true

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build
        run: ${{ matrix.use_cross && 'cross' || 'cargo' }} build --release --target ${{ matrix.target }}

      - name: Rename binary
        shell: bash
        run: |
          cp target/${{ matrix.target }}/release/${{ matrix.binary }} ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.target }}
          path: ${{ matrix.artifact }}

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
          generate_release_notes: true
